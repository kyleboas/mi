---
name: monitors
triggers:
  - manual: true
tools: []
permissions: {}
---
# Retired monitor registry

This file is kept only so old links have a clear answer. Mi no longer reads a configured monitor registry. `mi tick` does not consume health sidecars or start work from rows in this file.

The active iMessage repair monitor is fixed code in `src/imessage-monitor.ts`. See `docs/monitoring.md`.
